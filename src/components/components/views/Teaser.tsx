'use client';

import { Component } from '@/types/page-types';

export default function Teaser({ component }: { component: Component }) {
  return (
    <article className="teaser">
      <h1>Teaser</h1>

    </article>
  );
}