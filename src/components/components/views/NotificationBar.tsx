'use client';

import { Component } from '@/types/page-types';

export default function NotificationBar({ component }: { component: Component }) {
  return (
    <article className="notificationbar">
      <h1>notificationBar</h1>

    </article>
  );
}