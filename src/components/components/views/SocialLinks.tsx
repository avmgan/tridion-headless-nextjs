'use client';

import { Component } from '@/types/page-types';

export default function SocialLinks({ component }: { component: Component }) {
  return (
    <article className="sociallinks">
      <h1>SocialLinks</h1>

    </article>
  );
}