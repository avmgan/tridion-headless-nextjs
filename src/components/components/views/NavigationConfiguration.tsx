'use client';

import { Component } from '@/types/page-types';

export default function NavigationConfiguration({ component }: { component: Component }) {
  return (
    <article className="navigationConfiguration">
      <h1>navigationConfiguration</h1>

    </article>
  );
}